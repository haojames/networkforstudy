 # DHCP Server Configuration on Ubuntu

# Assign IPs to clients in subnet 10.10.10.0/24
subnet 10.10.10.0 netmask 255.255.255.0 {
    range 10.10.10.100 10.10.10.200;
    option routers 10.10.10.1;              # Gateway = FortiGate port2
    option domain-name-servers 8.8.8.8, 8.8.4.4;
    default-lease-time 600;
    max-lease-time 7200;
}

# Declare Ubuntu's own subnet (no IP assignment needed)
subnet 172.16.10.0 netmask 255.255.255.0 {
    # No range needed for this subnet
}
